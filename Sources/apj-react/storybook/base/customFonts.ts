import { css } from 'styled-components';

const rootFontStyles = css`
  :root {
    --font-primary: 'Arial';
    --font-primary-pro: 'Arial';
    --font-primary-light: 'Arial';
    --font-secondary: 'Arial';
    --font-pampers: 'El Messiri Regular';
    --zoom: 4px;
  }
`;

export default rootFontStyles;
